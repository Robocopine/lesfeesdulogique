{% extends 'base.html.twig' %}

{% block title %}Substance index{% endblock %}

{% block body %}
    <h1>Substance index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NameFr</th>
                <th>NameNl</th>
                <th>NameEn</th>
                <th>NameDe</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for substance in substances %}
            <tr>
                <td>{{ substance.id }}</td>
                <td>{{ substance.nameFr }}</td>
                <td>{{ substance.nameNl }}</td>
                <td>{{ substance.nameEn }}</td>
                <td>{{ substance.nameDe }}</td>
                <td>
                    <a href="{{ path('app_substance_show', {'id': substance.id}) }}">show</a>
                    <a href="{{ path('app_substance_edit', {'id': substance.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_substance_new') }}">Create new</a>
{% endblock %}
